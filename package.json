{
  "name": "lightweight-trading-bot-3-root",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "setup": "npm install && cd backend && cargo fetch",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && cargo build --release",
    "build": "npm run build:frontend && npm run build:backend",
    "backend": "cd backend && cargo run --bin lightweight-trading-bot-3",
    "frontend": "cd frontend && npm run dev",
    "dev": "npm run full",
    "full": "concurrently -k -n backend,frontend -c cyan,magenta \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
